namespace alpaka_cuda_async {
  void enumerate(bool verbose);
}

namespace alpaka_rocm_async {
  void enumerate(bool verbose);
}

namespace alpaka_serial_sync {
  void enumerate(bool verbose);
}

namespace alpaka_threads_sync {
  void enumerate(bool verbose);
}

namespace alpaka_tbb_sync {
  void enumerate(bool verbose);
}

namespace alpaka_syclcpu_async {
  void enumerate(bool verbose);
}

namespace alpaka_intelgpu_async {
  void enumerate(bool verbose);
}
